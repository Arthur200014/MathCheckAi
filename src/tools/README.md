# Tools — этап 1

На этапе 1 реальные tools ещё не подключены.
На следующих этапах сюда добавятся:

- `criteria.py` — `load_criteria(task_type)`
- `sympy_tools.py` — проверка выражений и корней
- `trig_circle.py` — детерминированная тригонометрическая окружность
- `memory.py` — SQLite history
- `report.py` — сохранение отчёта
